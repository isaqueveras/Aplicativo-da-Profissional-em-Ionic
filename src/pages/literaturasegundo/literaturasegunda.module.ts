import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { LiteraturasegundaPage } from './literaturasegunda';

@NgModule({
  declarations: [
    LiteraturasegundaPage,
  ],
  imports: [
    IonicPageModule.forChild(LiteraturasegundaPage),
  ],
})
export class LiteraturasegundaPageModule {}
